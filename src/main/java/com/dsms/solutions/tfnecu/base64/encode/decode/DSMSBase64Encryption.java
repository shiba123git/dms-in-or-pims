/**
 * 
 */
package com.dsms.solutions.tfnecu.base64.encode.decode;

/**
 * @author Babu
 *
 */
@SuppressWarnings("restriction")
public class DSMSBase64Encryption {

	/*
	 * public static String encryptData(String paramValue) { return (paramValue
	 * != null) ? Base64.encode(paramValue.getBytes()) :
	 * Base64.encode(paramValue.getBytes()); }
	 * 
	 * @SuppressWarnings("restriction") public static String decryptData(byte[]
	 * paramValue) { byte[] decodedValue = null; try { decodedValue =
	 * Base64.decode(paramValue); } catch (Base64DecodingException e) {
	 * e.printStackTrace(); } String decodedVaalueString = new
	 * String(decodedValue); return decodedVaalueString; }
	 */

}
